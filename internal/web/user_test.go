package web

import (
	"testing"
)

func TestSignup(t *testing.T) {
	// password := "123123"
	// res, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.MinCost)

	// ok := bcrypt.CompareHashAndPassword(res, []byte("11"))
	// fmt.Println(ok)
	// fmt.Println(string(res), err)
}
